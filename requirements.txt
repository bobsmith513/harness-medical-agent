# ============================================================================
# harness-medical-agent 基础部署依赖（mock 模式，零外部服务）
#
# 适用场景：本地演示 / CI / 快速验证
#   - 不连接任何外部服务（无 LLM / 无 Milvus / 无数据库）
#   - 4 个端到端 demo + 5 个模块级 demo + 634 项测试全部可跑
#
# 安装：
#   pip install -r requirements.txt
#   pip install -e .                # 项目本体（src-layout，必须安装）
# ============================================================================
# Python >= 3.10（推荐 3.10 / 3.11 / 3.12）

# ---- 核心框架（必装）----
langgraph>=1.2.11              # 主 Agent 编排（StateGraph 状态机）
pydantic>=2.7,<3               # 领域模型校验（Evidence / ReasoningChain / SessionContext）
pydantic-settings>=2.3,<3      # 配置系统（.env 环境变量绑定）
pyyaml>=6.0.3                  # 专家声明式配置（configs/experts.yaml）
